# TACTUS 蓝图 v2：基于 ds005662 的 EEG–视频多模态对比学习

**版本**: v2 终稿（2026-08-16。v1 经三路对抗性审查——统计/泄漏、ML 可行性、科学定位——后全面修订；审查纪要见 §11）
**数据集**: "A comprehensive EEG dataset for investigating visual touch perception" (Scientific Data 13:381, 2026; OpenNeuro ds005662 v2.0.1, **CC0**)
**代号**: **TACTUS** = **T**ouch **A**lignment by **C**ontrastive **T**ransfer to **U**nseen **S**ubjects
（拉丁语 tactus = 触觉；缩写展开直接编码本项目的头条声明——向未见被试的迁移。首选备名 TACIT 已被 arXiv 2602.07061 占用，弃用。）

---

## 0. 一句话定位（v2 重排）

> **首个"双重留出"（未见被试 × 未见视频）零样本 EEG–视频对齐**：利用该数据集独一无二的稠密共享设计——**80 名被试 × 完全相同的 360 个条件 × 8 次重复**——训练被试不变的共享嵌入空间，并以该设计独有的两件分析武器支撑：**朝向等变性几何分析**（4 朝向是数据集中唯一被正交操纵的因子）与**逐被试噪声上限归一化**（8 重复免费提供）。

三个贡献按可信度排序：

1. **主贡献（ML）**：双重留出零样本泛化 + 跨被试缩放曲线（LOSO 用 79 人训练，文献里全是 9–19 人）+ "新被试只需 k 个校准试次"的实用曲线。卖点是**稠密共享设计**（80×360×8），不是裸被试数（Défossez/Meta 已有 100+ 被试，但无稠密共享条件）。
2. **方法贡献**：时间分辨对齐头（分窗投影头产出"对齐起始曲线"，与作者组 MVPA 起始延迟直接可比）+ 朝向等变性分析（嵌入在刺激翻转下不变 vs 等变子空间的分解——ML 审稿人看重、且任何竞争数据集做不了）。损失函数部分诚实定位为工程选型而非贡献（见 §4.3）。
3. **支撑性科学终点**：（a）"大脑触摸编码匹配哪一层视觉表征、在什么时间窗"（传感器空间表述，不做通路解剖裁决）；（b）表型失谐设计——情感轴对齐预测 VT 分数、朝向轴对齐不预测、二者都不被 SNR 解释的**双重分离**（严格预注册控制后才可声明，见 §7）。

明确不做：视频重建（90 个同域刺激正中 spurious-reconstruction 批评靶心）；TPJ/pSTS vs S1 解剖裁决（64 通道头皮 EEG 无个体 MRI 属于过度延伸）；"负缩放结果同样可发表"的自我安慰（单数据集的平坦曲线是欠决定的，除非配上噪声上限与误差分解使其可解释）。

---

## 1. 数据集档案（全部仓库实查）

### 1.1 总体
| 项 | 值 |
|---|---|
| 被试 | 80 人（54女/24男/2非二元；18–76 岁，μ=30.1——年龄跨度大，既是混淆也是稳健性素材）|
| EEG | 64 通道 BioSemi ActiveTwo，2048 Hz，CMS/DRL，50 Hz 工频区，**0 条 EOG 通道**（sidecar 实查）|
| 试次 | 每人 2880 非目标试次（90 视频 × 4 朝向 × 8 重复 = 360 条件）+ 每序列 1–9 个目标试次（按键）|
| 呈现 | 32 个序列；SOA 精确 800 ms（600 ms 视频 + 200 ms ISI），RSVP 式 |
| 托管 | OpenNeuro ds005662 v2.0.1，BIDS，CC0，~120 GB |
| 下载 | `aws s3 sync --no-sign-request s3://openneuro.org/ds005662 .` 或 DataLad |

### 1.2 关键文件
- `sub-XX/eeg/*.bdf`：原始连续记录 ~1.3 GB/人（~110 GB 全套）。**v2 修订：原始数据是必需品而非可选项**——ICA 需要连续/1Hz 高通数据，derivatives 的 0.1Hz 高通+已基线校正 epoch 做不了眼动成分分析，且基线污染无法从 .fif 撤销（§6.2）
- `derivatives/mne/*_epo.fif`（~11 GB）：MNE 1.7，平均参考、0.1–100 Hz、200 Hz、−100~+800 ms epoch、−100~0 基线、**无伪迹剔除无 ICA、未确认 notch**——只够 Phase 0 体检用
- `events.tsv`：`onsetsample, istarget, presentationnumber, stimnumber(1–384), stim(Windows反斜杠路径), stimname, time_stimon/off, stimdur(~0.61s), rt, correct`。朝向与源视频号（1–90）从路径解析
- `code/analysis/VTD.csv`：90 行逐视频监督表——arousal、threat、连续 valence（PCA 复合）、pain(0/1)、touch_type(12)、toucher(hand/object)、object(28)、material(8)、approaching、英文动作描述
- OSF jvkqa：**350 名评分者的逐视频 Neutral/Pleasant/Unpleasant/Painful 计数分布**（v2 升级为一级资产：行为软标签 + 评分者分歧度 = 标签不确定性权重，见 §4.4）
- `phenotype/`：EQ(15题/满分30)、IRI(7题/满分35)、VT（**含 10 个探测视频的逐被试逐条评分 Feel_touch/Intensity/pleasant/unpleasant/painful/部位——数据集中唯一"标签与感知者同源"的地方**，v2 升级为 Q3 关键资产）、MTS（自报+自由文本）
- `participants.tsv`：VT_score、EQ_score、IRI_score、MTS 汇总列
- `code/experiment_files/stimuli/`：全部 384 个 mp4 随数据集分发

### 1.3 工程坑位清单
- `stim` 路径反斜杠需归一化；`duration` 列是名义 0.2s，真时长看 `stimdur`
- `stimnumber` 索引 384 全集，VTD 属性按源视频号 join
- **基线污染**：−100~0ms 基线窗落在上一试次 onset 后 700–800ms——上一试次的效价峰(300ms)/疼痛持续(240ms+)/威胁唤醒(230–260ms)响应全部拖过 700ms，基线校正等于给每个 epoch 减去一个"上一试次内容依赖"的常数
- **相邻 epoch 共享原始数据**：试次 t 的 700–800ms 就是试次 t+1 的基线段（200Hz 下 20 个样本物理相同）
- 目标试次后的试次带按键运动电位污染（rt/correct 列可定位，必须剔除）
- 600ms 视频 offset 响应（~600–750ms）在 epoch 内
- 50Hz 工频在 0.1–100Hz 通带内，notch 状态未确认，需 PSD QC

### 1.4 已发表分析足迹（基线与新颖性边界）
作者组（Smit, Ramirez-Haro, Varlet, Moerel, Quek, Grootswagers；WSU MARCS）：
1. Sci Data 2026 数据集论文（MVPA 技术验证）
2. **Imaging Neuroscience 2025 companion**（10.1162/IMAG.a.1017）：时间分辨 MVPA（正则化 LDA/岭回归，**留一序列 CV**）。解码起始/峰：手朝向 ~60ms（峰 120–130ms）、材质 110–120ms、效价起始 ~130ms（峰 300ms）、触摸类型 165ms、威胁/唤醒 230–260ms、疼痛 135ms 瞬态+240ms 持续。精度 modest，贝叶斯统计稳健化

全部线性 MVPA；无深度学习、无跨被试模型、无零样本。外部引用：0（Semantic Scholar 1 条为自引）。

---

## 2. 竞争格局与机会窗口

| 社区 | 现状 | 关系 |
|---|---|---|
| THINGS-EEG 对比解码（NICE→ATM→CognitionCapturer→2026 wave）| 被试内 200-way top-1 卷到 ~40%，LOSO 崩到 8–30%（全部 ≤10 被试）| 方法模板；**最可能抢先**（ds005662 已进 EEGDash 索引）|
| EEG-视频（SEED-DV 系）| 唯一 EEG-视频基准，20 被试、需授权 | 我们贡献第二个公开基准（触摸域、CC0、80 被试）|
| 替代性触觉神经科学（Smit/Keysers/Banissy/Lee Masson）| 单变量 ERP + 线性 MVPA；**个体差异是作者组明示的下一步** | 科学问题来源；Q3 竞争者——若他们先发"解码精度与 VT 组相关"，表型头条就被实质抢注（§8 的占位策略因此前置）|

唯一另一个公开观看触摸 EEG 数据集：Lee Masson & Isik 2023（21 被试、75 视频、OSF 5ntcj）——v2 定位：**探索性附录**，成功才升主文（承诺了却因蒙太奇工程失败会被读成反证；通道映射方案需在架构定型前决定，见 §9）。

---

## 3. 科学问题（v2 重写）

**Q1（因子化→等变性重构）**。原"材质×视角×效价是否因子化"不可回答：三者是同 90 个片段的相关属性（刀划=金属+威胁+负效价，8 材质×12 触摸类型需 96 格仅 90 个视频）。v2 拆成：
- **Q1a（可回答，新颖）**：朝向等变性——学到的 EEG 嵌入在刺激翻转下如何变换？分解不变子空间（内容）与等变子空间（朝向/视角），这是数据集唯一正交因子，任何竞争数据集无此设计
- **Q1b（描述性，有界）**：属性方差分割，明确声明受 90 片段属性相关结构限制；**Phase 0 先算 90×90 属性互相关矩阵并发表**，由它决定哪些对比可答

**Q2（通路→特征层级时间课程）**。放弃 TPJ/pSTS vs S1 解剖裁决（传感器空间 EEG 的反向推理，两类审稿人都会毙）。改为可辩护版本：**哪一层视觉表征（低层运动能量/纹理 → 中层手部接触结构 → 高层社会-情感语义）在哪个时间窗与 EEG 对齐**——用分窗对齐头（0–150 / 150–350 / 350–600ms）操作化，产出与作者组 MVPA 起始延迟直接可比的"对齐起始曲线"。与 Lee Masson & Isik 的时序发现（视觉 ~90ms → 社会情感 ~150ms）对话但不做解剖声明。源定位（模板 MRI eLORETA）最多探索性附录。

**Q3（表型失谐设计）**。核心风险（三位批评者一致命中）：per-subject 对齐分数单调于通用 EEG 可解码性（头骨/帽贴合/伪迹率/年龄），且训练目标本身在抹平被试差异，且规范性标签（350 外部评分者）把"偏离常模的被试"机械地记为低对齐。v2 协议见 §7——只有**双重分离**成立才作声明：情感轴对齐预测 VT，朝向轴对齐不预测，二者均不被 SNR 代理解释。

---

## 4. 模型框架（v2 修订）

### 4.1 视频侧普查（Phase 0，不训练先普查）
90 视频过三族冻结编码器：文本对齐族（SigLIP2 帧均值 / X-CLIP / InternVideo2-f4）、SSL 族（VideoMAE v2 / V-JEPA2 attentive-probe 协议）、触觉语义族（UniTouch 的 ImageBind 视觉塔、TVL 触觉形容词打分——**注意这两者训练域是第一人称传感器数据，对第三人称视频只救材质轴，期望要校准**）。产出 90×90 RDM vs 行为评分 RDM。
**新增关键测试（ML 批评者）**：leave-one-video-out 用 VTD 属性（和其余 89 个嵌入）预测被留出视频的冻结嵌入。**属性预测不了嵌入 ⇒ EEG 侧视频级零样本在原理上不可能**，立即降级为属性级泛化作主终点。这个普查本身可发 CCN 摘要占位（§8）。

### 4.2 EEG 侧
- 输入：**主窗口 0–600ms、无基线校正（逐通道 robust scaling 替代）**；−100~+800ms 降为敏感性分析（§6.2 理由）
- 主干：NICE/ATM 式浅层时空卷积 + 通道注意力（0.1–4M 参数）；EA 白化默认开
- 被试条件化（subject token / 1×1 conv 层 / SuLoRA 低秩适配器）三选一消融，**但注意 §7：表型测量用的是另一个无条件化的冻结编码器**
- **纯 LOSO 推断规则先验定死**（均值 token 或恒等适配器，写进预注册），否则 LOSO 从头条声明中删除
- EEG 基础模型（LaBraM/CBraMod/EEGPT）：全微调基线**只跑 3 折**，并记录 LaBraM 1s/200 样本 patch 与我们 120 样本 epoch 的接口错配（padding 决策成文）——定位为指示性基线

### 4.3 损失（v2 诚实化）
**承认结构性事实（ML 批评者）**：视频侧是 72–81 个冻结原型的固定码本，masked InfoNCE / masked SigLIP / SupCon / ProtoNCE 在去重后全部退化为同一个 ~72-way 原型分类目标——损失阶梯大概率是 null 消融。因此：
- **主目标 = 条件级原型对比**（单试次 EEG vs 视频原型），损失家族对比降为 2–3 格 sanity check，不再标榜"核心方法贡献"
- 方法新颖性预算转投真正结构性的东西：**跨被试机制 + 朝向等变性设计 + 时间分辨对齐头**（§0 贡献 2）
- 批构造：**分层条件采样**（每批 ≤90 个不同源视频、各取 1 试次、原型级去重负样本；CLISA 项负样本严禁与锚同序列——慢漂移会白送简单负样本）
- **朝向政策是目标函数的分叉不是消融细节**：翻转作负样本（逼编码器从 EEG 判别镜像→容量倒向低层/眼动轴）vs 翻转作正样本+独立朝向监督头（丢掉 Q1a 的判别信号）。Phase 1 用两格试点 + 后部通道 vs 前部通道分解（量化学到的朝向信号中眼动占比）后锁定
- 课程学习对策 SNR：先用 2–4 试次伪试次锚训练，退火到单试次（用 8 重复结构，机制性对策而非祈祷）
- λ 项:cross-modal 主项 + λ₁ CLISA 跨被试项（**注意其与跨模态项部分冗余**——固定视频锚已间接拉近跨被试 EEG；λ₁ 的净贡献必须单独消融）+ λ₂ RNC 连续效价轴 + λ₃ 文本/属性锚

### 4.4 第三模态与软标签（v2 升级）
- **SoftCLIP 软目标改用 350 评分者行为分布**（Neutral/Pleasant/Unpleasant/Painful 计数）而非视频编码器相似度行——人类知觉定义的分级相似度，绕开嵌入坍缩风险；评分者分歧度作为逐视频标签不确定性权重
- VTD 英文描述 + VLM 结构化 caption（NICE→NICE-LLM 有 +4pts 先例）
- 高分歧视频预测：规范性标签对齐应在高分歧视频上最弱（顺带检验典型性混淆，喂给 §7）

---

## 5. 评测协议（v2 大修：先注册后跑）

### 5.1 划分设计
- **零样本 = 源视频不相交**：held-out 视频的 4 朝向 × 8 重复 × 全体被试整体进测试；**朝向/重复级划分 = 泄漏**
- **分层只按材质**（8 类 × ~11 视频，每折每类 1–2 个可行；touch_type 12 类分层在算术上不可能——12×8=96>90，属性级零样本只在 ≥20 个随机划分上汇报合并结果，并公布空格折表）
- **时间邻接约束**：测试 epoch 与任何训练 epoch 不得共享原始样本或时间相邻（±1 试次）；剔除所有目标试次后继试次
- **双重留出格（主声明）**：5 视频折 × 8 被试折（每折留出 10 人）= **40 次训练**，每人恰好在每个视频折被留出一次 → 聚合成 n=80 被试级分数 + bootstrap CI。（原"≥10 视频折 × 80 LOSO"= 800 次训练不可行）
- 缩放曲线（10/20/40/79 训练被试）：**固定同一评估被试集**贯穿所有点，相邻点配对推断，预注册"可判定为上升"的斜率幅度与 CI；配噪声上限与刺激限/被试限误差分解，否则平坦曲线只是一个耸肩
- 模型选型与显著性报告分离：嵌套 CV 或留一个最终视频折做确认，防选型乐观偏差

### 5.2 统计推断（v2 新增纪律）
- **单一主终点预注册**：被试内、视频不相交、18-way top-1、伪试次 k=4、≥10 视频折聚合。其余全部挂层级 FDR；Q2 时间曲线用聚类置换 + 特征层 max-stat
- **置换单元 = 源视频**（可交换单元），绝不置换试次级标签（同视频 32 试次是簇，试次级置换的零分布窄 4–9 倍 = 制度化假阳性）
- **明确每个对比的推断目标**：模型排序/消融 = 固定刺激推断（n=80 被试级配对 Wilcoxon，同折）；刺激泛化声明 = 被试×视频交叉混合模型，且只在差距超过视频级最小可检差（90 视频单元、p≈0.15 时 MDD ≈ 8 个百分点）时才作。**THINGS 式 1–4 点的损失差异在本设计的视频级分辨率之下**——这就是 §4.3 降格损失阶梯的统计学理由
- 效应量刻度先记住：18-way chance=5.56%，单折单被试 18 视频单元的 chance 精确二项 95% CI ≈ 0–19%——单折不能区分 15% 与 chance；一切声明建立在折×被试聚合上
- **噪声上限无处不在**：8 重复的分半信度给出（a）逐被试可达检索上限（b）Q3 衰减校正分母（c）"modest 精度"相对上限而非相对 100% 的诚实重述——EEG-CLIP 文献几乎没人做，免费的差异化
- **属性捷径上限量化**：报告同材质干扰项 gallery 的检索精度 + "材质+运动能量"解码器能解释多少全 gallery 精度——90 个同手同景视频的"零样本"有多少是 8-way 材质码的重装，先自己量化，别让审稿人来量

### 5.3 基线阶梯
1. companion 论文 LDA/岭回归——**改用留整序列 CV 使其与我们严格同协议**（v1 只是近似可比）
2. CorrCA/ISC 空间滤波、SRM（80×360 共享条件是 SRM 理想领地；新被试只拟合投影）
3. EA + 线性
4. NICE、ATM 移植
5. LaBraM/CBraMod/EEGPT 全微调（3 折，指示性）
6. VT-Align 完整模型 + 组件消融
- 被试身份探针**与对齐保留率联合报告**（信息-不变性权衡曲线）——单独的"身份精度越低越好"可以靠摧毁一切信息来刷

---

## 6. 混淆控制（v2 强化）

### 6.1 Phase 0 三大审计（任何训练之前）
1. **序列×朝向×源视频交叉表**（统计批评者头号发现）：2880/32 = 每序列恰 90 试次，32 = 4 朝向 × 8 重复——算术强烈暗示**每序列可能单朝向呈现**。若朝向按序列成块：朝向 60ms 信号部分是区组/时段解码（Li 2021 机制）、跨翻转泛化沦为跨区组泛化、Q1a 等变性设计需重推导。交叉表无论结果如何都进论文；若成块则（a）朝向解码声明重caveat（b）序列作为交叉随机效应/回归掉序列号（c）等变性分析改在序列内可比对上做
2. **90×90 属性互相关矩阵**：决定 Q1b 哪些对比可答，先于一切属性声明
3. **视频嵌入可预测性测试**（§4.1）：决定视频级 vs 属性级零样本谁当主终点

### 6.2 信号层
- **主训练窗 0–600ms、无基线校正**（基线段=上一试次内容依赖噪声：污染 CLISA 正对、白耗编码器容量）；−100~+800 敏感性分析；rERP/反卷积作为 800ms-SOA 重叠的建设性处理（把继承自作者 derivatives 的缺陷变成我们的严谨性展品）
- **主管线预声明**：原始 BDF → 50Hz notch（PSD QC 确认）→ 1Hz 高通副本跑 ICA（眼动成分）→ 0.1Hz 高通主副本 → autoreject 固定超参 → 重新 epoch 0–600ms。备选管线全部降为敏感性分析。**这意味着 ~110GB 原始下载 + 80 人预处理是 Phase 0 的一部分，不是可选项**
- 剔除：目标试次、目标后继试次；重复次数（presentationnumber）作为控制回归量并顺带产出适应性分析（免费科学结果）

### 6.3 眼动（v2 重写——数据集 0 条 EOG 通道）
- 构造 HEOG/VEOG 替代（F7–F8；Fp1/Fp2 − 头皮均值）+ ICA 眼动成分分析（注明无参考 EOG 的局限）
- 前部通道消融模型作为敏感性分析
- **"无眼动混淆"的延迟声明只在前扫视窗（<~150ms）内做**——这是唯一有原则的辩护；持续窗（效价峰 300ms）声明必须携带眼动不可证伪的**具名限制**（不是脚注）
- 认清双向性：EOG 替代解码器含真实前部神经信号（对早成分反保守），打败它也排除不了后部位点的扫视尖峰电位体积传导——它是敏感性分析不是判决

### 6.4 视角≠翻转
self/other 由垂直翻转操作化，与最早最强的朝向信号（60ms）混淆，且若 6.1-1 证实按序列成块则进一步与区组混淆。视角声明必须跨朝向泛化 + 以 6.1-1 审计结果为条件。

---

## 7. Q3 表型协议（v2 新章节：失谐设计）

**结构性问题**（ML 批评者）：双重对比 + 被试适配器的目标函数就是在**抹平**被试间对齐差异——训练成功后残余对齐差异是优化残渣，主要相关物是数据质量（伪迹率、留存试次、依从性），而依从性又通过尽责性与问卷分数假相关。**适配器参数把解剖/帽位/阻抗滋扰与表型信号打包在一起，"适配器参数预测 VT"不可解释。**

**协议**：
1. **表型测量与不变性优化分离**：另训一个共享编码器（λ₁=0、无被试条件化），冻结，在留出试次上测 per-subject 对齐——表型读出永远不来自那个被优化去抹平差异的模型
2. **单一复合对齐分数预注册**；分半（奇偶试次）信度 + 衰减校正相关 + CI
3. **SNR 失谐控制**：先回归掉逐被试噪声上限、伪迹率、留存试次数、目标检测 RT/命中率（注意力/依从代理）、年龄、性别（VTD 验证已报告触摸分类的性别差异）、以及**朝向可解码性**（非社会控制维度）
4. **可发表的形式是双重分离**：情感轴对齐预测 VT ∧ 朝向轴对齐不预测 ∧ 二者均不被 SNR 代理解释。单一相关（哪怕 r=0.3）不可解释、不声明
5. **规范性标签问题的直接检验**：用 VT 问卷里 10 个探测视频的逐被试评分（数据集中唯一感知者-标签同源处）做小型被试内监督分析；预测高评分者分歧视频上规范标签对齐最弱
6. 适配器参数→问卷仅作嵌套 CV 预测 + 80 被试置换零分布，放探索性附录
7. MTS：80 人中预计 1–2 真阳性，只做个案描述，零推断
8. 功效诚实：n=80 在 r≥0.31 才有 80% 功效，文献同类效应 r~0.1–0.25；试次级混合模型只能压缩测量误差，被试间功效钉死在 n=80 渐近线上——**这就是为何 Q3 是支撑终点而非头条**，且失谐设计（定性模式）比效应量估计（定量）更可达

---

## 8. 路线图（v2 修订时间线与投稿）

### Phase 0 — 数据落地与三大审计（**3–4 周**，v1 的 1–2 周低估了原始数据管线）
- 全量下载（110GB 原始 + derivatives）；预处理管线跑通 80 人
- 复现 companion 论文 2–3 条解码曲线（管道正确性）
- §6.1 三大审计 + 视频编码器普查
- **Go/no-go**：≥1 族视频编码器 RDM 与行为 RDM 显著相关 ∧ 属性可预测 held-out 嵌入（否则属性级泛化转正）
- **产出即占位**：编码器普查 + 审计结果 → CCN/CoSyNe 摘要或 bioRxiv 短文（"大脑触摸编码匹配哪层视觉表征"的 RSA 领土宣示）

### Phase 1 — 最小可行对齐（**3–5 周**）
- 评测 harness 先行（§5 全套 + 混淆 battery 一次性建成）
- **OSF 预注册**：划分协议、主终点、Q3 失谐控制——同时防 p-hacking 审稿质疑、对两类竞争者盖时间戳，成本近零
- NICE 移植 + 原型对比主线；朝向政策两格试点后锁定
- **Go/no-go**：被试内主终点显著（视频级置换）∧ 前扫视窗（<150ms）信号在前部通道消融下存活
- 通过即挂 arXiv 占位

### Phase 2 — 方法与缩放（**6–10 周**）
- 双重留出 40 次训练格（§5.1）；缩放曲线（固定评估集+配对推断+噪声上限）
- 被试条件化三选一；λ₁ 冗余性消融；损失 sanity check（2–3 格）
- LOSO+k 校准试次曲线（BCI 社区叙事，头条素材）

### Phase 3 — 科学分析（与 Phase 2 并行）
- Q1a 等变性几何；Q1b 有界方差分割；Q2 分窗对齐起始曲线；Q3 失谐协议

### Phase 4 — 整合成文
- **单篇整合论文优先**（科学分析就是与"平庸 NICE 移植"的区别，拆两篇会把两篇都摊薄）；neuro 期刊文从裁掉的分析中后续衍生
- Lee Masson 迁移：探索性附录，成功才升主文

### 投稿目标（v2 现实化）
- ~~ICLR 2027~~（摘要 ddl ~1 个月后 vs Phase 0–2 需 12–19 周，算术不成立；带半成品去投正是自己害怕别人写的那篇平庸移植）
- **主选：ICML 2027（~2027-01 截稿，容纳 Phase 0–3 完整故事）**；备选 NeurIPS 2027
- 占位链：Phase 0 CCN 摘要 → Phase 1 arXiv → Phase 2-3 完整论文
- 科学衍生文：Imaging Neuroscience（与作者组同刊对话）

---

## 9. 计算与工程预算（v2 新增）

- 单次训练：0.1–4M 参数、~230k epoch，约 1–2 GPU·h——**瓶颈不是单次训练而是协议乘数**
- 主要开销：双重留出 40 次 + 缩放曲线（4 尺寸×多抽样×固定评估）+ 消融（在固定 EA=on 下只做损失×条件化因子设计、5 折；终版模型 10+ 折）≈ 数百 GPU·h 量级，单卡数周/多卡数天
- FM 全微调基线是最贵单行——限 3 折
- 隐性成本大头：~8 个视频编码器环境（各自 repo/权重/依赖）、80 人 ICA（每人数小时 CPU）、110GB 下载（OpenNeuro S3 快；OSF 澳洲区慢）
- 跨数据集通道映射（插值层 vs 共享子集）**在架构定型前决定**——决定编码器是否需要通道位置条件化

## 10. 风险登记表（v2）

| 风险 | 概率 | 对策 |
|---|---|---|
| 朝向按序列成块（6.1-1 审计阳性）| **未知，算术可疑** | 审计前置；成块则朝向声明降级+序列随机效应；等变性分析改序列内设计 |
| "NICE-on-ds005662" 抢先 | 中 | Phase 0 CCN 占位 + Phase 1 arXiv；差异化压在双重留出/等变性/噪声上限（移植管线做不出）|
| 作者组先发个体差异 | 中 | Q3 走失谐-双重分离路线（相关分析做不出）；预注册时间戳；引用定位互补 |
| 视频嵌入坍缩/属性不可预测嵌入 | 中 | Phase 0 普查+可预测性测试前置；败则属性级泛化转正、行为软标签替代编码器相似度 |
| 学到的是眼动不是神经编码 | 中-高（朝向轴尤甚）| 前扫视窗主张、前部消融、ICA 成分分析、具名限制；对比目标偏爱最易判别信号=眼动轴，朝向政策试点专门量化 |
| InfoNCE 单试次不收敛 | 中 | 原型对比主线+伪试次课程退火；companion 已证信号存在 |
| 缩放曲线平坦 | 中 | 预注册诊断（噪声上限+误差分解）使平坦可解释；不再声称"负结果同样可发表" |
| Q3 双重分离不成立 | 中-高 | 设计上 Q3 已是支撑终点；失谐协议本身（含规范标签检验）可作方法学贡献陈述 |
| 审稿人锚定 THINGS 200-way 数字 | 高 | gallery 梯度曲线+同材质干扰项+噪声上限归一化，主动重定义刻度 |

## 11. 对抗性审查纪要（三批评者 → v2 采纳）

| 批评者 | 致命发现 | v2 处置 |
|---|---|---|
| 统计/泄漏 | 32 序列=4×8 算术暗示朝向按序列成块；epoch 相邻共享原始样本+基线含上一试次内容；表型终点欠功效+规范标签混淆+SNR 混淆三重打击 | §6.1-1 审计前置；0–600ms 无基线主窗+邻接约束+目标后继剔除；§7 失谐协议 |
| 统计/泄漏 | 置换单元错误（试次级零分布窄 4–9 倍）；消融在视频级分辨率之下（MDD≈8pts）；800 次训练不可行 | §5.2 视频级置换+推断目标声明；损失阶梯降格；5×8=40 训练格 |
| ML 可行性 | 表型被自身目标函数训练掉；损失家族去重后全部退化为 ~72-way 原型分类；derivatives-only 与 ICA/眼动控制自相矛盾（原始数据是硬依赖）| §7 分离协议；§4.3 诚实化+新颖性预算转移；§6.2 原始管线入 Phase 0 |
| ML 可行性 | CLISA 项与跨模态项冗余；朝向政策是目标函数分叉；时间预算低估 2–3×；LaBraM 接口错配 | λ₁ 单独消融；两格试点；Phase 0/2 扩时+§9 预算；FM 限 3 折 |
| 科学定位 | 头条与自我声明的探索性终点矛盾；Q1 按设计不可答；Q2 解剖过度延伸；ICLR 时间线算术不成立；"负结果可发表"不成立 | §0 重排头条；Q1→等变性+有界分割；Q2→传感器空间特征层时序；ICML 2027+占位链；缩放诊断预注册 |
| 三者一致 | 噪声上限缺席；350 评分者分布未用；预注册缺席 | §5.2 上限无处不在；§4.4 行为软标签；Phase 1 OSF 预注册 |

## 12. 文献底仓（按用途）

**方法模板**：NICE (ICLR'24, eeyhsong/NICE-EEG)；ATM (NeurIPS'24, dongyangli-del/EEG_Image_decode)；CognitionCapturer (AAAI'25)；NICE-LLM (TNNLS'25, 文本+4pts)；EEG2Video/SEED-DV (NeurIPS'24)；CLIP-MUSED (ICLR'24, subject tokens)；Défossez 2023 (Nat MI, subject layer, brainmagick)；SuLoRA (arXiv:2510.08059)；CLISA (TAC'22)；CEBRA (Nature'23)；SupCon (NeurIPS'20)；UniCL (CVPR'22)；ProtoNCE (ICLR'21)；SoftCLIP (AAAI'24)；SigLIP (ICCV'23)；Rank-N-Contrast (NeurIPS'23)；SAMGA (arXiv:2604.17782, 粒度匹配)；SREA (arXiv:2602.01728)
**视频/触觉编码器**：VideoMAE v2；V-JEPA 2 (vjepa2, attentive-probe)；InternVideo2 (f4 变体)；X-CLIP；SigLIP2；UniTouch (CVPR'24)；TVL (ICML'24)；Touch-and-Go (NeurIPS'22)
**被试不变/对齐**：EA (He&Wu'20；Junqueira'24 系统评估 arXiv:2401.10746)；SRM (NeurIPS'15, brainiak)；CorrCA/ISC (Parra lab)；跨被试综述 (arXiv:2604.27033)；EEG2EEG (CogSci'23)
**尺度定标**：Meta scaling laws (arXiv:2501.15322——加被试不涨解码，加对齐机制才涨)；THINGS-EEG2 (NeuroImage'22, concept-disjoint 划分模板)；LOSO 数字汇总 (MindAlign arXiv:2605.24523)；FACED (123 被试, Sci Data'23)
**混淆批判（审稿人手册）**：Li 2021 TPAMI block-design；Ahmed CVPR'21；watermelon (arXiv:2405.17024)；低层统计混淆 (NeuroImage'24)；Shirakawa spurious reconstruction (arXiv:2405.10078)；伪试次优化 (bioRxiv 2023.10.04.560678, ~4 试次平均最优)；眼动混淆 (eNeuro'18)
**神经科学**：companion (Imaging Neurosci'25, PMC12624366)；Smit 2023 felt/seen (NeuroImage)；Smit 2025 VT 普查 (Sci Rep, 84% 普遍性)；Lee Masson & Isik 2023 (J Neurosci, OSF 5ntcj)；VTD (Behav Res Methods'25)；Keysers 2010 (NRN)；Blakemore 2005 (Brain)；Bufalari 2007 (P45)；Rigato 2019 (P45/P100 视角重映射)；mu 抑制零结果 (Neuropsychologia'19, N=252)；vicarious body maps (Nature'25)；fMRI 表征差异性预测共情 (Imaging Neurosci'24)
**EEG 基础模型（仅指示性基线）**：LaBraM (ICLR'24)；EEGPT (NeurIPS'24)；CBraMod (ICLR'25)；FM 怀疑论 (EEG-FM-Bench arXiv:2508.17742；AdaBrain-Bench 反例)
